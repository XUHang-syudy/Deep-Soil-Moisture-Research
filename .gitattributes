# Git LFS tracking for large binary files
# Track .docx files with Git LFS
02_文章手稿/*.docx filter=lfs diff=lfs merge=lfs -text

# Ensure Markdown files are treated as text and normalize line endings
*.md text eol=lf

# Ensure shell scripts have LF line endings and are executable
*.sh text eol=lf

# General text files for consistent line endings
*.txt text eol=lf
README.md text eol=lf
.gitignore text eol=lf
.gitattributes text eol=lf
